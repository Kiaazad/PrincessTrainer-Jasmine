init 10:
    define config.intra_transition = None
    define config.layers = [ 'bg', 'map', 'master', 'transient', 'screens', 'top', 'overlay' ]
    default preferences.text_cps = 80
    # define config.fix_rollback_without_choice = False
    define config.has_autosave = False
    define config.keep_running_transform = False


