in case you have set up Github desktop and the .gitignore is empty or does not exist. on the top menu go to `Repository>Repository Settings...>Ignored Files` and add these:
*.rpyc
cache/
saves/
traceback.txt
log.txt